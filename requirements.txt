# Dependencies are declared in pyproject.toml — that file is the single source
# of truth. Do not add version pins here; they will drift from package metadata.
#
#   pip install -e ".[dev]"     # library + preview extras + test tools
#   pip install -e ".[all]"     # library + all preview extras
#   pip install -e .            # core runtime only
#
# The following line keeps `pip install -r requirements.txt` working for old
# links and checkouts: it installs this project from pyproject.toml.
.
